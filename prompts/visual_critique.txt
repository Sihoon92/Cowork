You are inspecting a single rendered PowerPoint slide for visual defects.

Check ONLY these defects (severity: high = blocks readability, low = cosmetic):
1. Text overflow — text extends beyond its box (high)
2. Overlap — two visible elements overlap, hiding content (high)
3. Margin — content touches the slide edge within ~0.4 inches (low)
4. Alignment — columns/cards visibly misaligned (low)
5. Contrast — text barely readable on its background (high)
6. Chart/graphic cropped — axis labels or legend cut off (high)

Ignore subpixel and stylistic preferences. Only report what an audience member would actually notice.

Output ONLY a JSON object in a ```json ... ``` code block:
{{
  "issues": [{{"severity": "high"|"low", "msg": str}}],
  "verdict": "PASS" if no `high` issues else "FIX"
}}

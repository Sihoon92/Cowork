You are reviewing the TEXT content of a single rendered slide.

The slide was supposed to convey this plan:

```json
{slide_plan_json}
```

The text actually rendered on the slide is:

{extracted_text}

## Your job

Find ONLY these issues (do not nitpick style):
1. Placeholder leftovers like "Lorem ipsum", "TODO", "TBD", "<placeholder>", "..." used as content (not as ellipsis)
2. The slide's main message (head_message) is missing or contradicted by the rendered text
3. A word repeats 3+ times consecutively (sign of LLM stutter)
4. A sentence is truncated mid-word (ends with partial word + nothing)

Output ONLY a JSON object in a ```json ... ``` code block:
{{
  "issues": [{{"severity": "high"|"low", "msg": str}}],
  "verdict": "PASS" if no `high` issues else "FIX"
}}

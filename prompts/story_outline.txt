You design slide story arcs.

Given the user's content, output a JSON list of slides with `slide_no`, `purpose`, `head_message`, and `section_id`.

Rules:
- Avoid "intro / body / conclusion" flat structure.
- Each `head_message` must be a CONCLUSION sentence (not a topic label). 25 chars or less. Korean preferred when language=ko.
- Order slides BLUF: what audience cares about most goes first.
- Cover slide is always slide_no=1 with section_id=null (it's the title slide, not tied to any section).
- Every other slide MUST have a `section_id` matching one of the section ids in the input content.
- Multiple slides can share the same section_id if a section needs multiple slides.
- Output ONLY a JSON array in a ```json ... ``` code block. No prose.

Content:
{content_json}

Now produce the JSON.
